#include <iostream>
using namespace std;

int main()
{
    // // 1. replace ? with operator
    // int num = 3;
    // cout << num + num << "\n";                 // 6
    // cout << num * num - num << "\n";           // 6
    // cout << (num * num) - num << "\n";         // 6
    // cout << (num + num + num) - num << "\n";   // 6
    // cout << (num + num) - (num - num) << "\n"; // 6
    // cout << (num / num) * (num + num) << "\n"; // 6
    // cout << (num + num) + (num % num) << "\n"; // 6

    // // Assign The Values
    char a = 0;
    char b = 10;
    char c = 100;

    // Do Not Edit
    cout << b * c + a << "\n"; // 1000
}